All 35 tools use hardcoded templates and pattern matching — zero AI is involved in generating results for any of them.

Here's the breakdown in detail:

Group 1 — Pure template/string injection (the biggest offenders)

These tools have a pre-written library of paragraphs, hooks, frameworks, and word banks. When you click "Generate," they just pick a template and slot your topic/keyword into it. The output is predictable and will look the same every time for similar inputs:

Tool	What it actually does
YouTube Script Generator	Picks from 5 fixed HOOKS and 5 fixed INTROS matched to your selected tone/style. Every section is a pre-written paragraph with ${topic} dropped in
YouTube Title Generator	Runs your topic through 7 hardcoded title frameworks (How-To, Number/List, Curiosity Gap, Speed/Shortcut, etc.). No variation beyond the template
YouTube Description Generator	Picks one of 3 hook templates and one of 3 body templates. Keywords are string-injected
YouTube Channel Name Generator	Combines your niche keyword with pre-written suffix/prefix word banks (Hub, Studio, Nova, Spark, etc.)
YouTube Tag Generator	Appends a fixed list of suffixes (tutorial, tips, step by step, for beginners, etc.) to your keyword
YouTube Video Idea Generator	Template-based
YouTube Hashtag Generator	Template-based
TikTok Script Generator	Uses a NICHE_DATA lookup table with pre-written mistakes/tips per niche, slotted into fixed script templates
TikTok Hook/Caption/Bio/Username generators	All template-based
All Instagram generators	All template-based
AI Prompt Generator	Template-based
Midjourney Prompt Generator	Template-based
Group 2 — Fake scoring/data (heuristic, not real)

These tools display numbers and scores that look like real data but are actually calculated from simple math rules — not real search data or AI analysis:

Tool	What it actually does
YouTube Keyword Generator	"Volume" and "Competition" scores are calculated using a seeded pseudo-random formula based on word count and whether the phrase contains words like "how to" or "beginners." The scores are the same every time for the same input and are not connected to actual YouTube search data
YouTube SEO Score Checker	Runs regex pattern matching against your text (checks for power words, CTA phrases, hashtags). Fully rule-based
YouTube Title Analyzer	Scores titles against hardcoded word lists (POWER_WORDS, CURIOSITY_PHRASES, EMOTIONAL_WORDS) using a points formula
Group 3 — Calculators (these are fine as-is)

These tools should be deterministic — they apply real formulas:

YouTube/TikTok/Instagram Money Calculators, CPM Calculator, Engagement Calculators, Shorts Revenue Calculator, Thumbnail Downloader — all correct and expected behavior.
The backend AI setup — exists but is completely bypassed

There is an OpenAI integration in toolEngine.ts with properly written AI prompts for title generation, tag generation, description generation, channel names, and more. But because every single tool has a custom frontend component registered in the tool registry, the backend API is never called. The AI layer exists but is dormant.

--- Every creative tool (scripts, titles, descriptions, keywords, hooks, captions, bios) produces template output, not AI output. The same topic typed twice gives nearly identical results. There's no intelligence, context awareness, creativity, or accuracy — just string interpolation dressed up with a loading spinner. 